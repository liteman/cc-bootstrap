Read the workflow guide at `.claude/workflows/$ARGUMENTS.md` and summarize it.

If the file does not exist, list available workflows by checking `.claude/workflows/` and suggest the closest match.

After reading, provide a brief summary of:
- Workflow purpose and when to use it
- Key steps in the process
- Important checkpoints or gates
