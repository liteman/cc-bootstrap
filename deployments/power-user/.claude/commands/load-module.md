Read the module guide at `.claude/modules/$ARGUMENTS.md` and summarize it.

If the file does not exist, list available modules by checking `.claude/modules/` and suggest the closest match.

After reading, provide a brief summary of:
- Module purpose
- Key files and patterns
- Important constraints or conventions
